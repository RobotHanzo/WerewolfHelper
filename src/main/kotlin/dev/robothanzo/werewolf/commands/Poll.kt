package dev.robothanzo.werewolf.commands

import dev.robothanzo.jda.interactions.annotations.slash.Command

/**
 * Legacy command placeholder. Poll lifecycle and logic are now handled by
 * the ExpelService and corresponding model classes.
 */
@Command
class Poll {
    // Intentionally empty — behavior moved to services
}
